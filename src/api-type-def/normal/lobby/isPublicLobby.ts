/**
 * Integer lobby names are public
 *
 * @returns {boolean} - boolean
 */
declare const isPublicLobby: () => boolean;
export { isPublicLobby };

/**
 * Integer lobby names are public
 *
 * @returns {boolean} - boolean
 *
 * isPublicLobby();
 */
