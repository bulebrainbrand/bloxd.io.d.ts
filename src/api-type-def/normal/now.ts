/**
 * Obtain Date.now() value saved at start of current game tick
 *
 * @returns {number}
 */
declare const now: typeof Date.now;

export { now };

/**
 * Obtain Date.now() value saved at start of current game tick
 *
 * @returns {number}
 *
 * now()
 */
