import { PlayerId } from "@type";

/**
 * Get all the player ids.
 *
 * @returns {PlayerId[]}
 */
declare const getPlayerIds: () => PlayerId[];

export { getPlayerIds };

/**
 * Get all the player ids.
 *
 * @returns {PlayerId[]}
 *
 * getPlayerIds()
 */
