import { EntityId, PlayerId } from "@type";
// what is entityEId????
// bro,it's entityEntityId
/**
 * Remove following entity from player
 *
 * @param {PlayerId} playerId
 * @param {EntityId} entityEId
 * @returns {void}
 */
declare const removeFollowingEntityFromPlayer: (
  playerId: PlayerId,
  entityEId: EntityId,
) => void;

export { removeFollowingEntityFromPlayer };

/**
 * Remove following entity from player
 *
 * @param {PlayerId} playerId
 * @param {EntityId} entityEId
 * @returns {void}
 *
 * removeFollowingEntityFromPlayer(playerId, entityEId);
 */
