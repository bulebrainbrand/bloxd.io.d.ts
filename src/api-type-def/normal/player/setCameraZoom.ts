import { PlayerId } from "@type";

/**
 * Set camera zoom for a player
 *
 * @param {PlayerId} playerId
 * @param {number} zoom
 * @returns {void}
 *
 * setCameraZoom(playerId, zoom);
 */
declare const setCameraZoom: (playerId: PlayerId, zoom: number) => void;
export { setCameraZoom };
/**
 * Set camera zoom for a player
 *
 * @param {PlayerId} playerId
 * @param {number} zoom
 * @returns {void}
 *
 * setCameraZoom(playerId, zoom);
 */
