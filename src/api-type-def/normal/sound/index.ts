import { broadcastSound } from "./broadcastSound";
import { playClientPredictedSound } from "./playClientPredictedSound";
import { playSound } from "./playSound";

export const SoundApis = {
  broadcastSound,
  playClientPredictedSound,
  playSound,
};
